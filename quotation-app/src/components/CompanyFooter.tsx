import React from 'react';
import type { CompanyConfig } from '../config/companies';
export const CompanyFooter: React.FC<{ company: CompanyConfig; companyId: string; salesName: string; salesMobile: string }> = ({ company, companyId, salesName, salesMobile }) => <div className="contract-party"><div className="stamp-container"><img src={`/jt-cai-quote/stamps/${companyId}.png`} alt="發票章" className="company-stamp" /></div><p>甲&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;方：{company.fullName}</p><p>法&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;代：{company.representative || ''}</p><p>住&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;址：{company.address}</p><p>統一編號：{company.taxId || ''}</p><p>電&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;話：{company.phone} &nbsp;&nbsp; {company.fax && `傳真：${company.fax}`}</p><p>業務代表：{salesName} &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; 行動電話：{salesMobile}</p></div>;
