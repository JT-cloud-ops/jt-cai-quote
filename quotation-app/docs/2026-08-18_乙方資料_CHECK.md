---
project: "乙方資料"
date: 2026-08-18
status: complete
---

# 乙方資料驗證紀錄

相關文件：[[2026-08-18_乙方資料_SPEC]] · [[2026-08-18_乙方資料_SDD]] · [[2026-08-18_乙方資料_PLAN]]

## 規格符合性

- [x] 八個乙方欄位可輸入並即時預覽。
- [x] `partyBDatabase` 可保存、搜尋與帶入。
- [x] 歷史紀錄與 JSON 匯入／匯出包含乙方資料。
- [x] 舊資料缺少 `partyB` 時可正常載入。
- [x] 列印預覽顯示乙方且空值安全。

## 驗證命令與結果

### Task 3：預覽與列印乙方資料

- RED：`npm.cmd test -- --run src/components/QuotationPreview.test.tsx`
  - 實際結果：2 個測試失敗，原因為 `TypeError: getPartyBPreviewLines is not a function`；證明預覽尚未有可驗證的乙方列值 helper。
- GREEN：`npm.cmd test -- --run src/components/QuotationPreview.test.tsx`
  - 實際結果：1 個測試檔、2 個測試通過；使用 `react-dom/server` 實際渲染預覽，確認八個乙方欄位值會出現在合約區。
- 密度縮放測試：`npm.cmd test -- --run src/domain/quotationCalculations.test.ts src/components/QuotationPreview.test.tsx`
  - 實際結果：2 個測試檔、3 個測試通過；長乙方資料的密度分數高於空白乙方資料，並降低版面縮放比例。
- 回歸驗證：
  - `npm.cmd test -- --run`：2026-08-18 執行，3 個測試檔、12 個測試通過；退出碼 0。
  - `npm.cmd run lint`：2026-08-18 執行，ESLint 無錯誤；退出碼 0。
  - `npm.cmd run build`：2026-08-18 執行，`tsc -b && vite build` 成功，33 個模組完成建置；退出碼 0。
  - `node scripts/verify-print-layout.mjs`：2026-08-18 執行，列印版面檢查通過（16/16）；退出碼 0。

### 舊資料相容性

- `src/domain/partyB.test.ts` 驗證缺少 `partyB` 的歷史資料會正規化為八個空白欄位。
- `src/components/QuotationPreview.test.tsx` 驗證缺少 `partyB` 的預覽不會輸出 `undefined`。

## 風險與判定

主要風險為既有歷史 JSON 缺少新欄位，以及合約區版面高度變化。前者已由正規化與舊資料測試覆蓋；後者已由密度縮放測試、production build 與列印版面腳本覆蓋。未發現新增風險。

最終判定：通過。

