---
title: 報價匯入資料驗證檢查
project: 報價匯入資料驗證
date: 2026-08-24
status: complete
tags:
  - project/估價單系統
  - validation
---

# 報價匯入資料驗證檢查

相關文件：[[2026-08-24_報價匯入資料驗證_SPEC]] · [[2026-08-24_報價匯入資料驗證_SDD]] · [[2026-08-24_報價匯入資料驗證_PLAN]]

## 規格符合性

- [x] 已確認原子拒絕匯入策略。
- [x] 已確認接受已知舊格式並補安全預設值。
- [x] 已確認採用自寫集中式 parser，不新增第三方依賴。
- [x] parser 與匯入入口已實作。
- [x] 驗證命令已執行。

## 已確認設計

- URL 與 JSON 檔案匯入共用 parser。
- 驗證錯誤包含欄位 path 與繁體中文訊息。
- 驗證失敗不更新既有報價資料。
- JSON 大小上限 1 MB；陣列與文字欄位有明確上限。

## 目前證據

- 修改前 `git status --short -- 1_Projects_專案/01_估價單系統`：無輸出。
- 修改前 HEAD：`8bc323525b1716aeb24deef5acf795ddc4e72db6`。
- 實作前備份 tag：`backup/quotation-app-pre-quotation-import-validation-implementation-20260824-171500`。

## RED/GREEN 證據

- RED：`npm.cmd test -- --run src/domain/quotationValidation.test.ts`；因 `quotationValidation.ts` 尚不存在而失敗。
- GREEN parser：同一命令 14/14 通過。
- 入口與回歸：`npm.cmd test -- --run`；4 個測試檔、26 個測試通過，退出碼 0。

## 完整驗證

- `npm.cmd run lint`：退出碼 0，ESLint 無錯誤。
- `npm.cmd run build`：退出碼 0，34 個模組完成 Vite production build。
- `node scripts/verify-print-layout.mjs`：退出碼 0，16/16 通過。
- `git diff --check`：退出碼 0。

## 實作檔案

- `src/domain/quotationValidation.ts`、`src/domain/quotationValidation.test.ts`
- `src/App.tsx`
- `src/components/QuotationForm.tsx`

## 環境備註

- 專案 `.gitignore` 會忽略 `*.json`，因此隔離 worktree 沒有本機 tsconfig；為完成 build，暫時補入與主工作區相同的 `tsconfig.json`、`tsconfig.app.json`、`tsconfig.node.json`，三者均未加入 Git commit。

## 風險

- 舊版 JSON 的實際欄位差異需要以既有測試與歷史資料 fixture 驗證。
- 數字欄位目前以字串保存，parser 必須避免把現有合法輸入誤判為非法。
- URL 解碼失敗與檔案讀取失敗需維持既有入口的使用者體驗。

## 最終判定

通過。匯入資料驗證已完成，失敗匯入維持原子拒絕，舊格式相容與錯誤訊息均有測試覆蓋。
